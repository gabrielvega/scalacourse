package org.scalacourse

import spray.json.DefaultJsonProtocol._

object Models {
/*
  implicit val projectFormat = jsonFormat6(Project)
  implicit val taskFormat = jsonFormat3(Task)

  case class Project(id: Int, name : String, leader: String, start: String, finish: String, task: Seq)
  case class Task(name : String, user: String, status: String)
  */
}